import React from "react";


// Delete http protokollalla.
// Täytyy saada tieto mikä muistiinpano poistetaan.
// Tänne vai notesiin?
const DeleteName = (props) => {

    return ( 
        <div>
            <button type="submit">delete</button>
        </div>
    );
}


export default DeleteName;